const { GRADE } = require('../grading')

const standardTokens = [
  '{{FootnoteStart}}',
  '{{FootnoteEnd}}',
  '{{InsertFootnotes}}',
]

const lint = (veevaToken) => {}

module.exports = {
  lint,
}
